🧠 單調棧 Monotonic Stack 面試速查表
一、先背這個總原則（超重要）

當題目在問：

「下一個更大 / 更小」

「往左 / 往右第一個符合條件的元素」

「區間貢獻、延伸到哪裡被擋住」

👉 第一反應：單調棧

二、四大經典題型對照表（必背）
題目在找什麼	Stack 型態	while 條件
Next Greater (右邊第一個更大)	遞減棧	cur > st.top()
Next Smaller (右邊第一個更小)	遞增棧	cur < st.top()
Previous Greater (左邊第一個更大)	遞減棧	cur >= st.top()
Previous Smaller (左邊第一個更小)	遞增棧	cur <= st.top()

📌 記憶口訣

👉 想找「更大」 → stack 留「小的」
👉 想找「更小」 → stack 留「大的」

三、三種最常見模板（直接背）
① Next Greater Element（不循環）
vector<int> nextGreater(vector<int>& nums) {
    int n = nums.size();
    vector<int> res(n, -1);
    stack<int> st; // 存 index

    for (int i = 0; i < n; i++) {
        while (!st.empty() && nums[i] > nums[st.top()]) {
            res[st.top()] = nums[i];
            st.pop();
        }
        st.push(i);
    }
    return res;
}


📌 特徵：

只走一次

不需要 % n

② Next Greater Element（循環陣列 ⭐）
vector<int> nextGreaterElements(vector<int>& nums) {
    int n = nums.size();
    vector<int> res(n, -1);
    stack<int> st;

    for (int i = 0; i < 2 * n; i++) {
        int idx = i % n;

        while (!st.empty() && nums[idx] > nums[st.top()]) {
            res[st.top()] = nums[idx];
            st.pop();
        }

        if (i < n) {
            st.push(idx);
        }
    }
    return res;
}


📌 循環題三大重點

i < 2*n

idx = i % n

i < n 才 push

③ Daily Temperatures（距離型）
vector<int> dailyTemperatures(vector<int>& t) {
    int n = t.size();
    vector<int> res(n, 0);
    stack<int> st;

    for (int i = 0; i < n; i++) {
        while (!st.empty() && t[i] > t[st.top()]) {
            int idx = st.top();
            st.pop();
            res[idx] = i - idx;
        }
        st.push(i);
    }
    return res;
}


📌 差別：

存的是「距離」

用 index 相減

四、進階題型（面試常見）
🧱 Largest Rectangle in Histogram

問「一根柱子能延伸多寬」

關鍵：

找 左邊第一個更小

找 右邊第一個更小

👉 用 遞增棧

五、什麼時候存 index？什麼時候存 value？
情況	存什麼
要算距離	index
要更新答案位置	index
只比大小	value

📌 90% 題目 → 存 index

六、面試官最愛問的「為什麼」
Q1：為什麼是 O(n)？

👉 每個元素 最多進 stack 一次、出 stack 一次

Q2：為什麼循環題要走 2n？

👉 第一輪放人
👉 第二輪找答案

Q3：為什麼第二輪不 push？

👉 不製造新需求，只解決舊問題

七、單調棧「秒殺判斷 checklist」

看到題目時，快速問自己：

 是否找「第一個符合條件」

 是否只在乎左右方向

 是否可以線性掃描

 是否不需要回頭

👉 全中 = 單調棧

八、終極一句話模板（背起來）

使用單調棧維持序列性質，
當遇到破壞單調性的元素時，
不斷 pop 並更新答案，
每個元素最多進出一次，
整體時間複雜度為 O(n)。
